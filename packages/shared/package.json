{
  "name": "@psp/shared",
  "private": true,
  "version": "0.0.1",
  "description": "Shared types and constants (rule-ID constants live here)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
