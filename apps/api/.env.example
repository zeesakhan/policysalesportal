# Environment: dev | uat | production (production refuses to boot with PLACEHOLDER_ values)
NODE_ENV=development
APP_ENV=dev
PORT=3001

# Real values arrive with credentials (BUILD-PLAN M5-T1); placeholders gate launch (CLAUDE.md §2)
DATABASE_URL=PLACEHOLDER_postgres_connection_string
